<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Kept in the debug source set so HTTP is never enabled in release. -->
    <application android:networkSecurityConfig="@xml/network_security_config" />
</manifest>
